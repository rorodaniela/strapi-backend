{
  "routes": [
    {
      "method": "GET",
      "path": "/applicants",
      "handler": "applicants.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/applicants/count",
      "handler": "applicants.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/applicants/:id",
      "handler": "applicants.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/applicants",
      "handler": "applicants.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/applicants/:id",
      "handler": "applicants.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/applicants/:id",
      "handler": "applicants.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
